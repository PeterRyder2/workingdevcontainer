name: Build and Push Docker image to Docker Hub


env:
  REGISTRY: ghcr.io
  IMAGE_NAME: actionstest

on: push
jobs:
  say_hello:
    runs-on: ubuntu-latest 
    steps:
      - name: say hello
        run: echo "Hello POOPOPOOPOPOPOPPOOPOPOPOP"

  # push_to_hub:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Login to Docker Hub
  #       uses: docker/login-action@v3
  #       with:
  #         username: peterryder3
  #         password: dckr_pat_nTx5HTti5e0daN7dDKaBqMqEajc
  #         # username: ${{ secrets.DOCKERHUB_USER }}
  #         # password: ${{ secrets.DOCKERHUB_TOKEN }}
  #     - name: say hello
  #       run: "echo Current Working Directory: $PWD"

  #     - name: Build and push Docker image
  #       uses: docker/build-push-action@v4
  #       with:
  #         context: ${{github.workspace}}/Dockerfile
  #         push: true
  #         tags: peterryder3/actionstest:prod
  push_to_registry:
    name: Push Docker image to Docker Hub
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4
      

      - name: Push to Docker Hub
        uses: docker/build-push-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          repository: peterryder3/actionstest
          tags: latest
 
